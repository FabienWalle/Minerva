{% extends 'emails/base_email.html.twig' %}

{% block content %}
    <div>
        <h1 class="text-xl font-bold">Confirmation d'emprunt</h1>
        <p>Bonjour,</p>
        <p>Vous avez emprunté le livre <strong>{{ bookTitle }}</strong>.</p>
        <p>Votre e-mail : {{ userEmail }}</p>
        <p>Date d'emprunt : {{ borrowingDate|date('d/m/Y') }}</p>
        <p>Date de retour prévue : {{ dueDate|date('d/m/Y') }}</p>
        <p>Bonne lecture ! 📚</p>
    </div>
{% endblock %}